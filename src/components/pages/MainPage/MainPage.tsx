import { MainTemplate } from 'components';
import { MainContainer } from 'containers';

export const MainPage = () => {
  return (
    <MainTemplate>
      <MainContainer />
    </MainTemplate>
  )
}
